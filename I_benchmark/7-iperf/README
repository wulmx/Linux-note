1. native test
 1) operate at server
 ./test.sh

  2) unify the benchmark directory where client and server are located


2. cmd
  # iperf -c 90.1.1.2 -i 1 -t 100000 -u -p 5001 –l 800
    -l  length  unit is Kbyte   default is 8KB
    -b  bindwidth   default is 1Mbps
    -u  UDP   default is TCP
    -t  time  default is 10s
    -i  interval
  # iperf -s 90.1.1.2 -i 1 -t 100000 -u -p 5001
